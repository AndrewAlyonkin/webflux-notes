spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/postgres?schema=users
    username: root
    password: root

# Ќастройка уровн€ логировани€ здесь не сработает, нужно настраивать его в logback-test.xml
logging:
  level:
    org:
      springframework: INFO